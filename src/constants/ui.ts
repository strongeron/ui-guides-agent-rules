/**
 * UI Constants
 *
 * Centralized magic numbers and timing values used across components.
 * Extract these to make them discoverable and maintainable.
 */

// Timing constants (in milliseconds)
export const COPY_FEEDBACK_DELAY_MS = 2000;
export const SIDEBAR_FOCUS_DELAY_MS = 100;

// Layout constants
export const SIDEBAR_WIDTH_CLASS = 'w-80';
export const SIDEBAR_HEADER_HEIGHT_PX = 130;
