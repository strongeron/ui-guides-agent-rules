logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets — never commit tokens/keys. `npm run check:secrets` is the backstop.
.env
.env.*
!.env.example
*.pem
*.key

# Local Netlify folder
.netlify

# Local MCP and agent config (contains absolute paths)
.mcp.json

# Ralph loop state (local runtime)
.ralph/

# Serena workspace state (local runtime)
.serena/
coverage/
playwright-report/
test-results/
.playwright/
.playwright-mcp/

# Local task tracking (untracked, kept out of the repo)
.beads/

# Generated validation artifacts + screenshots
screenshots/
doc/*-results.json
doc/comprehensive-validation.*
doc/validation-report.md
doc/validation-results.json
doc/visual-validation-*
doc/agent-rules-alignment.json
